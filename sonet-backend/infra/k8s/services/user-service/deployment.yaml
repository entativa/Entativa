apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: sonet
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: sonet/user-service:latest
        ports:
        - containerPort: 8081
        env:
        - name: ENV
          value: "production"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: sonet
spec:
  selector:
    app: user-service
  ports:
  - port: 8081
    targetPort: 8081
